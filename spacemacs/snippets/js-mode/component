# name : React component template
# contributor : Seong Yong-ju <sei40kr@gmail.com>
# key: component
# --

import { Component } from 'react';

import './`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`.scss';

const COMPONENT_ROOT = '${1:...}';

export type Props = {};

export default class `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))` extends Component<Props> {
  render() {
    return ${0:<div className={COMPONENT_ROOT} />};
  }
}
