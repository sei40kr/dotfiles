# -*- mode: sh -*-

# create_gh
# author: Seong Yong-ju <sei40kr@gmail.com>

create_gh() {
    local repo_name="$1"

    mkdir -p "${HOME}/develop/workspace/${repo_name}" &&
        ( cd "${HOME}/develop/workspace/${repo_name}"
          git init
          hub create "$repo_name" )
}
