# -*- mode: sh -*-

# ranger-cd
# author: Seong Yong-ju <sei40kr@gmail.com>

ranger-cd() {
    local tmpfile="$(mktemp -t tmp.XXXXXX)"

    ranger --choosedir="$tmpfile" "$@"

    if [[ -f "$tmpfile" ]]; then
        cd "$(cat "$tmpfile")"
    fi
    rm -f "$tmpfile"
}

ranger-cd "$@"
