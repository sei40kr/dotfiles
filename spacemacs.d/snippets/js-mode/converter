# -*- mode: snippet -*-
# name : WSP entity converter
# key: converter
# --

const convert${1:Entities} = (${2:records}: ${3:RecordType}[]) => ({
  allIds: $2.map((${4:record}: $3) => $4.${5:id}),
  byId: Object.assign({}, ...$2.map(($4: $3) => ({ [$4.$5]: $4 }))),
});
$0
